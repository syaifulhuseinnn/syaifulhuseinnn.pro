---
const headings = await Astro.props.headings;
---

{headings && headings.length > 0 && (
  <nav class="toc my-8 rounded-lg border p-4 dark:border-gray-700">
    <h2 class="text-lg font-semibold">Table of Contents</h2>
    <ul class="mt-4 space-y-2 text-sm">
      {headings.map(heading => (
        <li class="line-clamp-1 hover:text-indigo-600 dark:hover:text-indigo-400">
          <a href={`#${heading.slug}`} class="block">
            {heading.text}
          </a>
        </li>
      ))}
    </ul>
  </nav>
)}